global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "ticket-booking"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets:
          - "host.docker.internal:8080"

# Prometheus: GET http://host.docker.internal:8080/actuator/prometheus -> Periodically scrape at this http endpoint.